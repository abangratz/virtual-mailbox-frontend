= form_for @transport do |f|
  -if @transport.errors.any?
    #error_explanation
      %h2= "#{pluralize(@transport.errors.count, "error")} prohibited this transport from being saved:"
      %ul
        - @transport.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
